# Keep the Docker build context minimal.
# Only the Dockerfile and entrypoint are needed for the image.

# Ignore everything by default
**

# Allowlist required files
!Dockerfile
!entrypoint.sh
!.dockerignore

# Optional: unignore docs if you plan to COPY them
#!README.md
#!LICENSE

# Note: compose files, tests, VCS metadata, env files, temp/data dirs, etc.
# remain excluded by the blanket ignore above.
